{{#each eventNamespacesByDocDir}}
{{#each this}}
#start-file:{{#if documentationDir}}{{documentationDir}}/{{/if}}{{snakecase name}}.txt
{{#each allVersions}}

{{event}}

    V{{version}}
    Неймспейсы: {{join additionalNamespaces ', '}}
    {{#each (splitByNewLine description)}}
    {{this}}
    {{/each}}
    {{#if comment}}
    
    {{/if}}
    {{#each (splitByNewLine comment)}}
    {{this}}
    {{/each}}

    {{#each parameters}}
    {{@index}}. {{name}}: {{>typeFormat parameter=this}}
    {{#each (splitByNewLine description)}}
       {{this}}
    {{/each}}
    {{/each}}
{{/each}}
{{/each}}

#end-file
{{/each}}
