{{#each eventNamespacesByDocDir}}
{{#each this}}
#start-file:{{#if this.documentationDir}}{{this.documentationDir}}/{{/if}}{{snakecase this.name}}.txt
{{#each allVersions}}

{{#unless (isEmpty additionalNamespaces)}}
{{#each additionalNamespaces}}
{{this}}.{{../event}}
{{/each}}
{{else}}
{{event}}
{{/unless}}

    V{{this.version}}
    {{this.description}}

    {{#each this.parameters}}
    {{@index}}. {{this.name}}: {{>typeFormat parameter=this}} - {{this.description}}
    {{/each}}
{{/each}}
{{/each}}

#end-file
{{/each}}
