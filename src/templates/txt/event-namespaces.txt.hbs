{{#each eventNamespacesByDocDir}}
{{#each this}}
#start-file:{{#if documentationDir}}{{documentationDir}}/{{/if}}{{snakecase name}}.txt
{{#each allVersions}}

{{event}}

    V{{version}}
    Неймспейсы: {{join additionalNamespaces ', '}}
    {{description}}

    {{#each parameters}}
    {{@index}}. {{name}}: {{>typeFormat parameter=this}} - {{description}}
    {{/each}}
{{/each}}
{{/each}}

#end-file
{{/each}}
