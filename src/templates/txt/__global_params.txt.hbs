.GlobalParams

    {{globalParameters.description}}

{{#each globalParameters.parameters}}
    {{@index}}. {{this.name}}: {{>typeFormat parameter=this}}
    {{#each (splitByNewLine description)}}
       {{this}}
    {{/each}}{{this.description}}
{{/each}}
